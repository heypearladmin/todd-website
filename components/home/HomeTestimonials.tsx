/**
 * Retired. Replaced by HomeSocialProof in the current home composition.
 * Kept as a no-op export so any stray imports still resolve cleanly.
 */
export function HomeTestimonials() {
  return null;
}
