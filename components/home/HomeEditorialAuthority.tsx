/**
 * Retired. Replaced by HomeVideoAuthority in the current home composition.
 * Kept as a no-op export so any stray imports still resolve cleanly.
 * Safe to delete when you are sure nothing imports it.
 */
export function HomeEditorialAuthority() {
  return null;
}
